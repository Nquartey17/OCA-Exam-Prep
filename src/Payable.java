public interface Payable {
    //When an interface doesn't have methods, it's called a marker interface
    //Functional interface has one abstract method
    public abstract double raiseSalary(double percentage);
}

